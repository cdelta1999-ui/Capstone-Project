# Dependencies for the Salifort Motors Streamlit dashboard.
# Kept as lower-bound pins so Streamlit Community Cloud can resolve wheels that
# match its Python runtime. Use Python 3.12 in the app's advanced settings.
# streamlit>=1.58 guarantees the width="stretch" chart/dataframe API the app uses.
streamlit>=1.58
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
plotly>=5.24
