# Dependencies for the Salifort Motors Streamlit dashboard.
# Lower-bound pins chosen to be widely available on PyPI and mutually
# compatible, so Streamlit Community Cloud can always resolve wheels for its
# Python runtime. Works on Python 3.9-3.12.
streamlit>=1.28
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
plotly>=5.18
